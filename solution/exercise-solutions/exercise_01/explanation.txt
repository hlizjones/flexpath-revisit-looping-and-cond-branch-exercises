Explanation:

The function uses logical && and || operators to combine conditions.
The strict inequality !== ensures that banned is not true (it could be undefined).
The function checks for age, admin status or subscription type, and ban status.